#include "Datalink.h"
#include <cstring>
#include <iostream>
using namespace std;

int main() {
  Datalink link = new Datalink();
  char* packet = link.recv
  char* ret;
  memcpy(ret, packet, 64);
  return ret;
}
